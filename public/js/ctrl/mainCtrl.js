(function() {
  'use strict';
  angular.module('quizter')
    .controller('mainCtrl', mainCtrl);

  function mainCtrl(mainFcty, quizFcty, $state, $rootScope) {
    var vm = this;

    // var plainRef = firebase.database().ref().child('object');
    // plainRef.on('value', snap => console.log(snap.val()));





  }


}());
